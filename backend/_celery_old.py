# ไฟล์นี้ถูกทิ้งว่างเพื่อไม่ให้ shadow package celery ของ pip
# Celery app อยู่ใน celery_app.py
# This file is intentionally empty to avoid shadowing the celery pip package.
# The real Celery app is defined in celery_app.py
