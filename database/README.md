# Database Assets

Thu muc nay chua du lieu DB phuc vu van hanh va backup:

- `seed/`: file seed data (`words.csv`, scripts tao du lieu mau).
- `backup/`: file SQL backup de restore khi can.

Ghi chu:

- Lenh `seed_full_catalog` uu tien doc `database/seed/words.csv`.
- Van giu fallback path cu de tranh vo luong cu.
